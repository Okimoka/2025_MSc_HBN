#import "utils/todo.typ": TODO

== Use of AI tools

// Write about the usage of AI tools if any - specify details. E.g.:

#TODO()[Parts of the code associated with this Master thesis were produced with the assistance of AI tools. Specifically, in <context>, <tool> was used, for the purpose of <purpose> in sections of the file `<file>`.]
// No AI tools were employed for the production of the written thesis or that of the materials used for the associated presentation.

== Reproducibility and Tips <app:repro-tips>

#figure(caption: [A view inside the finalized merged dataset])[
  #image("image-9.png", width: 60%)
] <fig:blur_plot_small>


== Synchronization metrics <app:sync_metrics>

#TODO()[finish this]

Not all captured metrics were eventually used for the final subject curation. 

#figure(
  table(
    columns: 2,
    table.header(
      [metric name],
      [used],
    ),

    [test1], [yes],
    [test2], [no],
  ),
  caption: [Metric usage],
)

